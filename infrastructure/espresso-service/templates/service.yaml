apiVersion: v1
kind: Service
metadata:
  name: {{ include "espresso-service.fullname" . }}
  labels:
    {{- include "espresso-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "espresso-service.selectorLabels" . | nindent 4 }}
