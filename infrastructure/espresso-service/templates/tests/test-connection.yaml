apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "espresso-service.fullname" . }}-test-connection"
  labels:
    {{- include "espresso-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "espresso-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
